<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= board.name %></title>
  <style>
    .break {
      page-break-after: always;
    }

    .clear {
      clear: both;
    }

    .element {
      width: 250px;
      margin: 0 1% 1% 0;
      float: left;
      box-sizing: border-box;
      overflow: hidden;
    }

    .relation {
      text-align: center;
      height: 125px;
      padding: 20px;
      font-size: 18px;
      border: 5px solid #000;
    }

    .item {
      height: 250px;
      padding: 20px;
      border: 5px solid #000;
    }

    .user {
      width: 75px;
      height: 75px;
      line-height: 75px;
      margin: 0 1% 1% 0;
      float: left;
      box-sizing: border-box;
      overflow: hidden;
      color: #FFF;
      background-color: #000;
      font-size: 50px;
      text-align: center;
      vertical-align: text-top;
    }
  </style>
</head>
<body>
  <% let e = 0; %>

  <% for (let i = 0; i < relations.length; i++) { %>
    <% if (e > 20 && i % 2 === 0) { %>
      <div class="clear break"></div>
      <% e = 0%>
    <% } %>
    <div class="element relation"><%= relations[i].name %></div>
    <% e += 2.5 %>
  <% } %>

  <% e+= 2 %>
  <div class="clear"></div>

  <% for (let i = 0; i < items.length; i++) { %>
    <% if (e > 20 && i % 2 === 0) { %>
      <div class="clear break"></div>
      <% e = 0%>
    <% } %>
    <div class="element item"><%= items[i].name %></div>
    <% e += 5 %>
  <% } %>

  <% if (team) { %>
    <% for (let i = 0; i < team.users.length; i++) { %>
      <div class="user"><%= team.users[i].initials.toUpperCase() %></div>
    <% } %>
  <% } %>
</body>
</html>